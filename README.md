# E-commerce-recommendation-Engine-using-Flask
AI-powered product recommendation web app built with Flask and Sentence-BERT. It loads precomputed product embeddings, takes a natural-language query from a search box, computes cosine similarity with all products, and returns the top-k most relevant items with titles, brands, categories, similarity scores, and images in a clean Bootstrap UI.
